export enum ImageType {
  AVATAR = 1,
  LOGO = 2,
  BANNER = 3,
  ATTACHMENT = 4,
}
export enum OwnerType {
  USER = 1,
  SERVER = 2,
}

export enum UploadStatus {
  start = 1,
  uploadSuccess = 2,
  uploadFail = 3,
}
